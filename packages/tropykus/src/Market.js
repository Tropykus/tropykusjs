import { ethers } from "ethers";

export default class Market {
    constructor(contractAddress, ethersProvider) {
        this.ethersProvider = ethersProvider;
        this.instance = new ethers.
    }
}